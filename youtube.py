import pytube

def download(url: str, path: str) -> int:
    '''
    Return a value that is 0 if the download is successful and -1 if it is not
    the function above has (url: str, path: str) -> int this is called type hinting
    it tells devlopers that the function takes two arguments url and path both of type string
    and returns an integer value

    Your code goes here
    All the best :)
    '''